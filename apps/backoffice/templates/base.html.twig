<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Gestor de Aplicaciones y Despliegues{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('assets/style.css') }}">
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <header>
        <h1>Gestor de Aplicaciones y Despliegues</h1>
    </header>
    
    <nav>
        <ul>
            <li><a href="{{ path('') }}" {% if route == '' %}class="active"{% endif %}>Dashboard</a></li>
            <li><a href="{{ path('credentials') }}" {% if route == 'credentials' %}class="active"{% endif %}>Credenciales</a></li>
            <li><a href="{{ path('packages') }}" {% if route == 'packages' %}class="active"{% endif %}>Paquetes</a></li>
            <li><a href="{{ path('configuration') }}" {% if route == 'configuration' %}class="active"{% endif %}>Configuración</a></li>
        </ul>
    </nav>
    
    <main>
        {% block content %}{% endblock %}
    </main>
    
    <footer>
        <p>&copy; {{ "now"|date("Y") }} Gestor de Aplicaciones y Despliegues</p>
    </footer>
    
    {% block javascripts %}{% endblock %}
</body>
</html>